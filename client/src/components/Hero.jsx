import React from "react";

export default function Hero() {
  return (
    <section>
      <h2>MERN Authentication</h2>
      <p>
        This is for MERN applicaton that stores a JWT in an HTTP-Only Cookie. It
        also consists of Redux Tookit
      </p>
      <button>Sign In</button>
      <button>Sign Up</button>
    </section>
  );
}
